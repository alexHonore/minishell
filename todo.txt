Makefile:
    path to the .h for compilation, add -I./pathtoheader in your compile flags as well so you don’t need the path in #include “foo.h”

Parsing:


"1123abc321", "12"


ignore ctr d and ctrl \

>> append > ecrase
cat << heredoc??   cat < infile

